apiVersion: v1
kind: Service
metadata:
  name: aiops-agent
  namespace: aiops
  labels:
    app: aiops-agent
spec:
  selector:
    app: aiops-agent
  ports:
    - port: 9090
      targetPort: 9090
      protocol: TCP
  type: ClusterIP
