[project]
name = "data-worker"
version = "0.1.0"
description = "Python FastAPI data processing service with intentional OOM fault"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.32",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
