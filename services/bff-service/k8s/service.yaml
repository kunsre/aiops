apiVersion: v1
kind: Service
metadata:
  name: bff-service
  namespace: aiops
  labels:
    app: bff-service
spec:
  selector:
    app: bff-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
